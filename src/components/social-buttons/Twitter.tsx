import Button from "../button";

const TwitterIcon = () => {
  const onTwitterClick = () => {};

  return <Button label="Login with Twitter" onClick={onTwitterClick} />;
};
export default TwitterIcon;
